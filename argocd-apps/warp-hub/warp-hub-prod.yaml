apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: warp-hub-production
  annotations:
    argo.octopus.com/project: "warp-hub"
    argo.octopus.com/environment: "production"
    argo.octopus.com/instance: "demo.octopus.app"
spec:
  project: default
  source:
    repoURL: https://github.com/robpearson/kubernetes-apps.git
    targetRevision: HEAD
    path: helm-charts/warp-hub/test
  destination:
    server: https://kubernetes.default.svc
    namespace: warp-hub-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - Validate=false
      - CreateNamespace=true
