apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: aetherflow-test-kustomize
  namespace: argocd
  annotations:
    argo.octopus.com/project: "aetherflow"
    argo.octopus.com/environment: "test"
    argo.octopus.com/instance: "demo.octopus.app"
spec:
  project: default
  source:
    repoURL: https://github.com/robpearson/kubernetes-apps.git
    targetRevision: HEAD
    path: kustomize/aetherflow/overlays/test
  destination:
    server: https://kubernetes.default.svc
    namespace: aetherflow-test
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - Validate=false
      - CreateNamespace=true
