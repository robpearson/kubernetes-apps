apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: aetherflow-prod-kustomize
  namespace: argocd
  annotations:
    argo.octopus.com/project: "aetherflow"
    argo.octopus.com/environment: "production"
    argo.octopus.com/instance: "demo.octopus.app"
spec:
  project: default
  source:
    repoURL: https://github.com/robpearson/kubernetes-apps.git
    targetRevision: HEAD
    path: kustomize/aetherflow/overlays/prod
  destination:
    server: https://kubernetes.default.svc
    namespace: aetherflow-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - Validate=false
      - CreateNamespace=true
